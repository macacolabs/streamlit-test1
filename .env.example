OPENAI_API_KEY=
BACKEND_API_KEY=
BACKEND_URL=

# 인증 (streamlit-authenticator)
# AUTH_PASSWORD_HASH: bcrypt 해시값 발급 명령어
# python -c "import bcrypt; print(bcrypt.hashpw('비밀번호'.encode(), bcrypt.gensalt()).decode())"

AUTH_USERNAME=admin
AUTH_PASSWORD_HASH=
TAVILY_API_KEY=